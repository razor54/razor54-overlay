name: PR auto manifest

on:
  workflow_dispatch:

jobs:
  disabled-note:
    runs-on: ubuntu-latest
    steps:
      - name: Explain current limitation
        run: |
          echo "Automatic pkgdev manifest updates are currently disabled."
          echo "Reason: the GitHub Actions environment used here does not provide a working pkgdev setup out of the box."
          echo "For now, generate manifests locally on a Gentoo system with pkgdev manifest and push them to the PR branch."
